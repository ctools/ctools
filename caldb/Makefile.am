## Process this file with automake to produce Makefile.in

# Calibration files to distribute
irfs = cta_dummy_irf.dat

caldbdir = $(datadir)/caldb/cta
caldb_DATA = $(irfs)

# Extra files to be distributed in the tar file
EXTRA_DIST = $(irfs)
